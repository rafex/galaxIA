{
  "name": "@rafex/galaxia-satellite-capabilities-wasm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Puerto AssemblyScript (WASM real) de packages/satellite-capabilities — aritmética y cómputo de CURP, pensado para un Ephemeral Satellite (nodo WASM en navegador, ver spec-native/DECISIONS.md). No comparte código fuente con el paquete TS por incompatibilidades de dialecto entre tsc y asc; se verifica réplica exacta contra ese paquete vía el mismo caso dorado.",
  "scripts": {
    "build": "asc assembly/index.ts --outFile build/satellite-capabilities.wasm --optimize --exportRuntime",
    "typecheck": "tsc --noEmit && asc assembly/index.ts --noEmit",
    "lint": "eslint src/",
    "test": "npm run build && vitest run"
  },
  "devDependencies": {
    "@assemblyscript/loader": "0.28.20",
    "@rafex/galaxia-satellite-capabilities": "^0.1.0",
    "@types/node": "20.19.43",
    "assemblyscript": "0.28.20",
    "typescript": "5.9.3"
  }
}
