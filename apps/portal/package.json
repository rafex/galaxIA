{
  "name": "@galaxia/portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Frontend del chat comunitario FHS v0.1. Vite + vanilla TypeScript + HTML5 + CSS3.",
  "scripts": {
    "dev": "vite",
    "build": "sh ../../helpers/scripts/shell/gen-version.sh 2>/dev/null || true; tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No linter configured yet'"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.3.0"
  },
  "dependencies": {
    "@rafex/galaxia-fhs-protocol": "^0.1.0"
  }
}
