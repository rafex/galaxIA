# ROADMAP.md

## Ahora

- **Completado (DEC-0038):** separación de repos — `galaxIA` queda como protocolo/SDK/servicios core (Atlas, Navigator, Portal); las implementaciones de referencia de providers (`star-example`, `satellite-ocr-example`, `rag-provider`, `kb-provider`) se movieron a [`galaxIA-satellite-star`](https://github.com/rafex/galaxIA-satellite-star) — 2026-07-06. `@galaxia/fhs-protocol` se consume ahí vía git (`github:rafex/galaxIA#fhs-protocol-dist`), no publicado a npm todavía.
- **Completado (DEC-0039):** renombrado a `@rafex/galaxia-fhs-protocol` y publicado a GitHub Packages en cada push a `main` que lo modifique (`.github/workflows/publish-fhs-protocol.yml`) — 2026-07-06. `spec-native/pipelines/CI.md`/`CD.md` pasaron de templates vacíos a documentos reales; `CI.md` documenta honestamente que no hay validación automatizada de PRs todavía.
- **Completado (DEC-0040):** `galaxIA-satellite-star` migrado a consumir `@rafex/galaxia-fhs-protocol` vía GitHub Packages (ya no la dependencia git `fhs-protocol-dist`) — 2026-07-06. Se encontró y corrigió un bug real: la versión `0.1.0` publicada en DEC-0039 no incluía `dist/` (npm respeta `.gitignore` sin un campo `files`), corregido en `0.1.1`. Autenticación vía `GH_TOKEN` (`.npmrc` con `${GH_TOKEN}`); los `Containerfile` de providers usan un secret de BuildKit (no build-arg) para no dejar el token en ninguna capa de imagen — verificado con `podman history`. (La sincronización de versión hacia `galaxIA-satellite-star` **no es un pendiente de este repo** — ver DEC-0041: `galaxIA` publica el paquete, cómo y cuándo un consumidor externo se actualiza es responsabilidad de quien opera ese repo, mismo principio de DEC-0026/DEC-0037.)
- **Completado (DEC-0041):** bump de versión del protocolo y verificación del tarball automatizados en el workflow de publicación (`helpers/shell/`, `helpers/python/` con `uv`, `helpers/mk/protocol.mk`) — 2026-07-06.
- **Completado y verificado en producción (DEC-0042):** CI real de typecheck/build/lint por Pull Request (`.github/workflows/ci.yml`) — 2026-07-06. Se encontraron y corrigieron 3 bugs reales solo visibles corriendo el workflow de verdad en GitHub Actions (nunca detectados en meses de desarrollo solo en macOS): script raíz `build` sin `apps/atlas` (gap desde DEC-0035), `dist/` de `fhs-protocol` faltante en checkout limpio, y `package-lock.json` sin el binario nativo de Rollup para Linux. Corrida final en verde: typecheck + build + lint completos en el runner real. **Pendiente (backlog, no bloqueante):** configurar branch protection en GitHub para que estos checks sean obligatorios; agregar un linter real (ESLint) ahora que el gate de lint ya existe.
- **Iniciativa activa:** `fhs-mvp` — MVP del protocolo FHS y chat comunitario para la ponencia.
  - Federar LLMs locales y tools MCP.
  - Frontend web vanilla con Vite.
  - Registry embebido en Agent Backend con WebSocket y SQLite.
  - Demo con OCR y failover de nodo.
- **Bloqueos actuales:** ninguno. El pipeline de OCR está verificado end-to-end con modelo real y ejecución determinística (DEC-0014, DEC-0015, DEC-0016, DEC-0017, DEC-0020 — ya no depende de que el LLM decida invocar la tool), y el aislamiento de eventos entre conversaciones concurrentes también está verificado (DEC-0018, probado con dos inferencias reales en paralelo). Se pausa temporalmente `SPEC-AUTH-0001` para enfocar recursos en el MVP.
- **Completado y verificado (DEC-0037):** `rag-provider` — indexado y recuperación de documentos por conversación (SPEC-RAG-0001) — 2026-07-06. `examples/rag-provider` implementa `document_index`/`document_query` con un motor mínimo (similitud de Jaccard, no embeddings semánticos — deliberadamente no una recomendación, DEC-0026). Integrado en `chat-ws.ts`/`runtime.ts` reutilizando el flujo de confirmación de OCR ya existente. Verificado real con procesos locales: indexado tras confirmación, recuperación en el turno siguiente, chunk correcto anteponer al mensaje, `provenance` reflejando el uso.
- **Completado y verificado (DEC-0037):** `kb-provider` — bases de conocimiento de solo lectura compartidas entre conversaciones (SPEC-KB-0001) — 2026-07-06. `examples/kb-provider` implementa `kb_query` cargando una carpeta de contenido al arrancar (mecanismo de prueba mínimo, explícitamente no un workflow de indexado recomendado — TASK-KB-0002 se cierra aclarando que la curaduría es responsabilidad exclusiva del operador, no una decisión de este proyecto). Modo manual (`preferences.kb`) y modo recomendado (matching determinístico contra `description`/`tags`, DEC-0028, con confirmación `kb.recommended`/`kb.decision`) verificados de punta a punta en navegador real. `kbMaxPerQuestion > 1` queda parcial (TASK-KB-0004) — solo 1 KB por pregunta soportado, con advertencia explícita si se pide más.
- **Diseño cerrado, sin implementar (DEC-0048, `spec-native/specs/kb-multi-consulta/SPEC.md` SPEC-KB-0002, extiende SPEC-KB-0001):** consulta de múltiples KBs por pregunta (issue #21) — catálogo visible + selección manual + matching determinístico top-N + LLM elige en el caso límite (sin match confiable), siempre con las KBs consultadas visibles al usuario (requisito no negociable que hace aceptable esa última parte sin reabrir DEC-0020). Fusión reutilizando RAG (`document_index`/`document_query`), requiere agregar un campo `source`/procedencia por chunk. **Explícitamente diferido:** prioridad normativa entre KBs (oficial vs. no oficial) — documentado, sin solución técnica asignada. Preguntas abiertas: umbral numérico, mecánica de calificación del usuario, si la elección del LLM en el caso límite usa tool-calling (mismo riesgo de confiabilidad de DEC-0016/DEC-0017).
- **Diseño cerrado, sin implementar (DEC-0049, `spec-native/specs/kb-citacion/SPEC.md` SPEC-KB-0003, extiende SPEC-KB-0001):** metadata de citación (`KbCitation`) y fuente primaria en resultados de `kb.query` — `documentTitle`, `sourceArtifact` (reutiliza `ArtifactRef`, DEC-0046), `sourceUrl`, `versionDate`, páginas, `tags`, y un bag libre `metadata: Record<string, string>` para campos específicos de dominio (jurisdicción, artículo, etc., sin que el protocolo imponga vocabulario). Aclaración de alcance explícita: esto define qué metadata puede *transportar* el protocolo, no cómo un provider *cura* su KB (sin cambios respecto a DEC-0026/DEC-0037/TASK-KB-0002).
- **Completado (DEC-0050, `spec-native/specs/parser-catalog/SPEC.md` SPEC-PARSER-0001):** `ModelParserProfile` — perfil declarativo de parseo tolerante por modelo (`ModelInfo.toolCalling.parserProfile`), transportando solo una referencia (`profileId`/`registryRef`) a conocimiento de comunidad sobre cómo tolerar inconsistencias de un modelo/motor de inferencia (ej. el caso ya documentado en DEC-0016/DEC-0017: `qwen2.5-coder-3b-instruct` escribiendo una tool call como JSON plano en `content`). El catálogo real (reglas de parseo, versión SQLite distribuible, trazabilidad de intentos con hash del contenido) vive fuera del protocolo, en [`galaxia-parser-catalog`](https://github.com/rafex/galaxia-parser-catalog) — mismo patrón que `ArtifactRef`/`capability.tags`. Primer perfil real (`jinja-plain-json-toolcall-fallback-v1`) implementado y `examples/star-example/src/llm-bridge.ts` (`galaxIA-satellite-star`) ajustado para usar el matcher genérico en vez de la función hardcodeada anterior — 2026-07-07. Pendiente (backlog, no bloqueante): mecanismo de distribución/actualización automática del catálogo hacia Stars, diseño completo del eval de falsos positivos/negativos, bump/publish de `@rafex/galaxia-fhs-protocol`.
- **Bug real encontrado y corregido (DEC-0037):** `McpHost.matchCapabilityId()` elegía la primera capability con alguna palabra en común con el nombre de la tool, no la de mayor coincidencia — con `rag-provider` (2 capabilities compartiendo el prefijo `document.*`) esto enrutaba mal `document_query`. Corregido a matching por score. También se corrigieron defaults de `REGISTRY_URL` en los Containerfiles de `star`/`satellite-ocr` (seguían apuntando a `navigator` en vez de `atlas`, herencia de DEC-0035) y el proxy de Vite en `apps/portal` (todo `/api` iba a Atlas en vez de dividir por prefijo).
- **Completado y verificado (local):** `satelite-rating` — mensaje `dispatch.ack` obligatorio (formaliza el "mosquito"/dispatcher concurrente de DEC-0009 con una señal verificable en el protocolo) + historial de latencia y rating por nodo en el Registry, expuesto en `GET /api/fhs/providers` (SPEC-SATRATING-0001 en `spec-native/specs/satelite-rating/SPEC.md`). Usa "nodo" como vocabulario de producto genérico ("estrella" para LLM, "satélite" para tools, DEC-0024). Verificado con un stack local real (agent-server + llm-provider + ocr-provider reales, FHS WebSocket real, LLM/OCR mockeados): rating y latencias correctos para chat y OCR. Pendiente (no bloqueante): repetir contra los 3 equipos reales de la demo.
- **Decidido (DEC-0024):** vocabulario espacial coherente con la marca "GalaxIA" — **Star**=nodo LLM, **Satellite**=nodo de herramientas, **Node**=término neutro; Atlas=Registry, Portal=chat web, Navigator=agente/orquestador, Beacon=manifiesto, Pulse=heartbeat, Flight Log=procedencia, Mission=ejecución de tool, Orbit=conexión activa, Signal=capability (SPEC-VOCAB-0001 en `spec-native/specs/vocabulario-espacial/SPEC.md`). Sin cambios de código, contenedores ni protocolo FHS. Documento canónico publicado en `docs/vocabulario.md` y frase de posicionamiento incorporada al portal web (`site/index.md`, sección "Estrellas y satélites") — 2026-07-05. Pendiente (no bloqueante, backlog): actualización retroactiva del resto de la documentación existente (TASK-VOCAB-0005).
- **Completado y verificado (Fase 1+2, DEC-0033):** rename de identificadores de código a la nomenclatura Galaxy/IA — 2026-07-06. Tipos TS (`Capability`→`Signal`, `ProviderManifest`→`Beacon`, `Registry`→`Atlas`, etc.), carpetas (`apps/agent-server/src/registry/`→`src/atlas/`), paquetes npm (`examples/llm-provider`→`examples/star-example`, `examples/ocr-provider`→`examples/satellite-ocr-example`) y contenedores (`fhs-llm-provider`→`fhs-star`, `fhs-ocr-provider`→`fhs-satellite-ocr`) renombrados. El protocolo JSON en el cable **no cambió** (mismas llaves/tipos de mensaje) — verificado con `npm run typecheck`/`build` en todo el monorepo y un chat E2E real local sin cambios de comportamiento. Fase 3 (renombrar el protocolo JSON en sí, ej. `providerId`) queda explícitamente fuera de alcance — requeriría actualizar todos los nodos reales a la vez (flag-day).
- **Completado y verificado (DEC-0034):** extensión del rename anterior — 2026-07-06. `apps/agent-server`→`apps/navigator` (`@galaxia/navigator`), `apps/web`→`apps/portal` (`@galaxia/portal`), contenedores `fhs-agent-server`→`fhs-navigator`, `fhs-web`→`fhs-portal`. De paso se corrigió un bug latente: los `Containerfile` de `star`/`satellite-ocr` seguían apuntando a `ws://agent-server:8081` (nombre de servicio que ya no existía tras el rename de examples en DEC-0033) — se habría roto en silencio un despliegue de un solo host. `docs/agent-server.md` renombrado a `docs/navigator.md`. Verificado con `npm run typecheck`/`build` en las 5 workspaces y un chat E2E real local.
- **Completado y verificado (DEC-0035):** Atlas (Registry) separado de Navigator (Agent Runtime) en dos servicios independientes — 2026-07-06. Nuevo `apps/atlas` absorbe el catálogo/registro (puerto 8081, sin cambio); `apps/navigator` (slim) le habla por HTTP vía `AtlasClient` (`getProviders()` lectura, `recordSample()` escritura fire-and-forget). Star/Satellite se registran directamente contra Atlas, sin cambios de protocolo. Hallazgo relevante: los eventos `node.online`/`node.lost` nunca tuvieron un consumidor real (`subscribeToRuntime()` no se llamaba en ningún lado) — no había nada que preservar al separar los procesos. Verificado real con 3 procesos `tsx` separados: resolución de provider vía HTTP, chat E2E completo, y métrica de rating confirmada en Atlas tras la llamada. Gap conocido documentado: el link HTTP Navigator→Atlas no soporta TLS con certificados autofirmados todavía (`AtlasClient` no tiene override de confianza). Pendiente no bloqueante: repetir la verificación en topología real de dos máquinas (bloqueado por hardware, mismo caso que issue #1).
- **Completado y verificado (DEC-0036, Parte 1):** rating de `satelite-rating` persistido en SQLite+WAL — 2026-07-06. `apps/atlas/src/atlas/metrics.ts` ya no pierde el historial de latencia/éxito en cada reinicio de Atlas (el catálogo de nodos sigue siendo en memoria a propósito, se reconstruye solo vía reconexión de providers). Excepción consciente documentada al precedente de DEC-0032 (bindings nativos): Atlas corre en la máquina core que el operador controla, no en hardware comunitario. Verificado real: proceso de Atlas matado y reiniciado, rating intacto sin ninguna llamada nueva.
- **Iniciativa en spec, sin iniciar (DEC-0036, Parte 2):** `atlas-navigator-bridge` — puente de eventos `node.online`/`node.lost` entre Atlas y Navigator vía NATS (SPEC-BRIDGE-0001 en `spec-native/specs/atlas-navigator-bridge/SPEC.md`). Documentado, no implementado — preguntas abiertas (NATS core vs. JetStream, snapshot inicial vs. solo deltas) sin resolver. No se inicia hasta decisión explícita de prioridad.
- **Completado y verificado:** rechazo de `hello` duplicado en el Registry (DEC-0009, primera mitad) — un `providerId` con una conexión activa (`readyState` real, no solo presencia en el mapa) ya no puede ser sobrescrito en silencio por una segunda conexión; responde `error { code: "ALREADY_REGISTERED" }` y cierra el socket nuevo. Una reconexión legítima tras una caída sigue funcionando sin bloqueo. Verificado con pruebas reales (duplicado simultáneo + reconexión tras `terminate()`). La segunda mitad de DEC-0009 (dispatcher/mosquito) sigue pendiente en `satelite-rating`.
- **Completado:** `ocr-confirmacion` — confirmación explícita (burbuja colapsada + botones) antes de que el LLM use el texto OCR (SPEC-OCRCONFIRM-0001, `spec-native/specs/ocr-confirmacion/SPEC.md`). Verificado end-to-end contra el bastion real. Sienta la base de estado por conversación que `rag-provider` también necesita (TASK-RAG-0001).
- **Completado y verificado:** topología multi-host — core (`web`+`agent-server`) en laptop (`192.168.3.137`), providers pesados (LLM/OCR) en bastion (`192.168.3.173`) (DEC-0022, `docs/despliegue-multi-host.md`). Desplegado y validado end-to-end con tráfico real cruzando ambas máquinas: chat simple y flujo completo de OCR con confirmación. Requirió además ajustar el mapeo de puertos de los providers (sin remapeo, para que coincida con lo anunciado en el manifiesto) y abrir reglas UFW en ambas máquinas, acotadas a la LAN.
- **Completado y verificado:** TLS/WSS con certificado autofirmado para todo el protocolo FHS (DEC-0023, `docs/tls-autofirmado.md`). Opt-in vía overlay `containers/compose.tls.yaml` — no afecta el despliegue sin TLS. Aplicado y validado end-to-end sobre la topología multi-host real (laptop + bastion): chat simple y flujo completo de OCR funcionando sobre el canal cifrado. Requirió tres ajustes de infraestructura (bind-mount con ruta absoluta configurable, puerto 8443 en vez de 443 por restricción de podman rootless, y recrear el túnel SSH del socket de podman) — ninguno del código TLS en sí.
- **Completado:** Pulse de transporte + timeout configurable del cliente (DEC-0010). El Registry sondea a cada nodo con ping/pong **nativo de WebSocket** (no un mensaje FHS) para detectar conexiones rotas más rápido que el lease de 30s (`apps/agent-server/src/atlas/ws-handler.ts`), tolerando 3 ciclos sin `pong` antes de terminar (no 1, para no ser más estricto que el lease de aplicación); se descarta explícitamente un mensaje de "progreso" de Mission (mantiene el Registry simple, DEC-0005) — es responsabilidad del nodo resolver internamente si se atoró, sin garantía de *cuándo*; y se agrega `preferences.maxWaitMs` en `ModelPreferences` (recomendado en rango de minutos, no segundos, dado el hardware comunitario) para que quien inicia la conversación pueda pedir esperar menos que el default fijo del stack. Verificado con `agent-server` real local. Pendiente no bloqueante: exponer `maxWaitMs` como control visible en el `Portal` (mismo patrón que `ocrMode`, DEC-0021).
- **Completado (lado Agent Server):** trazabilidad operacional (DEC-0012) — `apps/agent-server/src/observability/trace.ts` loggea una línea estructurada (`conversationId`, `requestId`, `providerId`, `capability`, `dispatchMs`, `totalMs`, `success`, `errorCode`) en cada Mission/chat resuelto, en éxito, timeout, error del nodo o cierre de conexión inesperado. Verificado end-to-end: `conversationId` de una sesión de chat real correlacionado con su línea de traza. Pendiente no bloqueante: que `llm-provider`/`ocr-provider` también loggeen su propia metadata local.
- **Completado:** validación de manifiesto + códigos de error estandarizados (DEC-0013) — el Registry rechaza (`INVALID_MANIFEST`) un manifiesto sin los campos obligatorios (`fhsVersion`, `provider.id/type/visibility`, `privacy.retention`, `privacy.trainingUse` si es `llm`, `endpoint`); `FHS_ERROR_CODES` en `packages/fhs-protocol` como códigos canónicos; `llm-provider`/`ocr-provider` migrados. Al activar la validación se descubrió que ninguno de los dos providers de referencia declaraba `privacy` en su propio manifiesto — corregido como parte de esta misma iniciativa. Verificado con `agent-server` real: manifiesto incompleto rechazado, `llm-provider` con el fix completa un chat E2E sin problemas.
- **Completado:** identidad Ed25519 real vía `did:key` (DEC-0030, reemplaza el DID simplificado de DEC-0004) — el `providerId` es la clave pública codificada (multicodec + base58btc), `hello`/`register` viajan firmados, el Registry verifica contra la clave derivada del propio `did` sin directorio de claves aparte. Usa `node:crypto` nativo (Ed25519 soportado desde Node 12+), sin dependencias nuevas. `llm-provider`/`ocr-provider` generan y persisten su identidad en un archivo local. Verificado real: identidad estable entre reinicios, chat E2E funcionando, y un ataque de suplantación simulado rechazado con `INVALID_SIGNATURE`. Pendiente no bloqueante: rotación/revocación de claves, recuperación de identidad perdida (no hay mecanismo, es limitación conocida).
- **Completado (local):** descubrimiento del Registry por mDNS, fase 1 (SPEC-P2P-0001, DEC-0032) — `bonjour-service` elegido con datos reales (sin bindings nativos, tipos TS propios, mantenimiento activo); el Registry también genera su propia identidad Ed25519 (DEC-0030) y firma su anuncio mDNS; `llm-provider`/`ocr-provider` descubren automáticamente si `REGISTRY_URL` no está configurado, verifican la firma, y opcionalmente anclan qué `did` de Registry esperan (`REGISTRY_EXPECTED_DID`). Verificado real: descubrimiento + registro end-to-end exitoso, y rechazo correcto con un `did` esperado incorrecto. Pendiente: TASK-P2P-0005 (documentación) y TASK-P2P-0006, **bloqueada por falta de acceso a hardware físico** (mismo bloqueo que issue #1).

## Después

- **Tests reales, formato/estilo, y workflow de regresión continua (SPEC-QUALITY-0001, `proposed`, DEC-0043):** hoy no hay suite de tests automatizada ni linter/formatter real configurado — solo typecheck/build/lint-placeholder (DEC-0042). El usuario pidió explícitamente diferir esto a una spec futura, a retomar después de cerrar las definiciones/decisiones activas del proyecto — no implementar ahora.
- **IPFS para artefactos (diseño cerrado, DEC-0044, `spec-native/specs/ipfs-adjuntos/SPEC.md` SPEC-IPFS-0001, sin implementar):** transporte de adjuntos configurable en el Portal — transmisión directa (default, síncrona) o vía IPFS (opt-in, el protocolo transporta solo el CID, el provider descarga en su propio momento/batch, anonimizando el origen del binario frente al provider). Retención default `PT3H`, ampliable explícitamente por quien sube el archivo. Preguntas abiertas: red pública vs. nodo privado del operador, granularidad de la elección (conversación vs. adjunto), quién sube el archivo, mecanismo de extensión de retención y de unpin.
- **Autenticación de usuarios:** retomar `SPEC-AUTH-0001` cuando el MVP esté estable.
- **Modelo de confianza comunitaria:** reputación, vetos persistentes y políticas de privacidad más granulares. Primera aplicación concreta diseñada (DEC-0028): tags de comunidad para contrastar contra `capability.description`/`tags` autodeclarados de una KB — bloqueado hasta que `SPEC-AUTH-0001` (identidad de usuario) se retome.
- **Nivel "premium" de nodo (documentado, sin implementar — DEC-0031):** campos declarativos adicionales (URL de código fuente, licencia, mantenedores, SLA, descripción de hardware, política de datos), mTLS entre ambos extremos, y un proceso de aprobación humana por parte de administradores de la red. Tensión explícita: introduce un punto de autoridad centralizada dentro de una red diseñada sin uno (DID autogenerado, DEC-0030) — trade-off consciente, no oculto. Cómo garantizar que el servicio en ejecución corresponde al código publicado queda **explícitamente pendiente de debate más profundo**, sin ninguna solución técnica asignada todavía.
- **Dispatcher/heartbeat concurrente en providers:** cada provider debe gestionar peticiones entrantes sin bloquear su propio `ping` (DEC-0009) — ahora en spec como `satelite-rating` (SPEC-SATRATING-0001), ver arriba.
- **SDK/provider de referencia en Python:** primer lenguaje no-TypeScript soportado (DEC-0011); caso de uso natural para providers de IA/OCR.
- **SDK/provider de referencia en Rust:** para hardware con recursos limitados (DEC-0011).
- **SDK/provider de referencia en Java:** para integración con sistemas comunitarios existentes (DEC-0011).
- **Evaluar modelo de chat general con tool calling** (`qwen2.5-3b-chat-q4` u otro) — hoy el modelo de producción es Coder 3B, suficiente para la demo de OCR pero no ideal para chat general (DEC-0016).

## Más adelante

- **Descubrimiento descentralizado (fase 1):** DHT/libp2p para descubrimiento fuera de la LAN queda como fase 2, sin spec todavía. La fase 1 (mDNS) ya está implementada y verificada en local — ver "Ahora".
- **Más tipos de proveedores:** `embedding`, `storage`, `resource`, `agent`.
- **NAT traversal:** permitir nodos fuera de la red local.
- **Gateway FHS en Rust/Rust-like:** versión ligera del protocolo para equipos pequeños.
- **Marketplace de tools:** catalogar y versionar capacidades ofrecidas por la comunidad.

## No hacer por ahora

- Frameworks frontend pesados (React, Vue, Angular) en el MVP.
- Soporte multi-idioma completo en la interfaz (solo español e inglés de los modelos/tools).
- Facturación, cuotas o monetización.
- Versionado de schemas de tools MCP fuera del alcance del protocolo.
- Reescribir el protocolo en Rust u otro lenguaje hasta que TypeScript demuestre validez.
