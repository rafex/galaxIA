# ROADMAP.md

## Ahora

- **Iniciativa activa:** `fhs-mvp` — MVP del protocolo FHS y chat comunitario para la ponencia.
  - Federar LLMs locales y tools MCP.
  - Frontend web vanilla con Vite.
  - Registry embebido en Agent Backend con WebSocket y SQLite.
  - Demo con OCR y failover de nodo.
- **Bloqueos actuales:** ninguno. El pipeline de OCR está verificado end-to-end con modelo real y ejecución determinística (DEC-0014, DEC-0015, DEC-0016, DEC-0017, DEC-0020 — ya no depende de que el LLM decida invocar la tool), y el aislamiento de eventos entre conversaciones concurrentes también está verificado (DEC-0018, probado con dos inferencias reales en paralelo). Se pausa temporalmente `SPEC-AUTH-0001` para enfocar recursos en el MVP.
- **Iniciativa en spec, sin iniciar:** `rag-provider` — indexado y recuperación de documentos por conversación (SPEC-RAG-0001 en `spec-native/specs/rag-provider/SPEC.md`). Extiende el patrón determinístico de DEC-0020 a la recuperación de contexto. Diseño cerrado el 2026-07-05 (DEC-0025): separado de memoria de conversación y de `kb-provider`, disparo de indexado solo por evento confirmado, estado por conversación resuelto (vive en `chat-ws.ts`, mismo patrón que `pendingAttachments`), retención/privacidad generalizadas (`privacy.retention`, `privacy.warning`). No se inicia hasta decisión explícita de prioridad.
- **Iniciativa en spec, sin iniciar (diseño menos maduro):** `kb-provider` — bases de conocimiento de solo lectura compartidas entre conversaciones (SPEC-KB-0001 en `spec-native/specs/kb-provider/SPEC.md`), para contenido público reutilizado por muchos usuarios (ej. la Constitución de México) que no debe pasar por RAG. Falta resolver cómo se dispara una consulta (no hay evento de "adjuntar" como en RAG) antes de considerarlo listo para implementación.
- **Completado y verificado (local):** `satelite-rating` — mensaje `dispatch.ack` obligatorio (formaliza el "mosquito"/dispatcher concurrente de DEC-0009 con una señal verificable en el protocolo) + historial de latencia y rating por nodo en el Registry, expuesto en `GET /api/fhs/providers` (SPEC-SATRATING-0001 en `spec-native/specs/satelite-rating/SPEC.md`). Usa "nodo" como vocabulario de producto genérico ("estrella" para LLM, "satélite" para tools, DEC-0024). Verificado con un stack local real (agent-server + llm-provider + ocr-provider reales, FHS WebSocket real, LLM/OCR mockeados): rating y latencias correctos para chat y OCR. Pendiente (no bloqueante): repetir contra los 3 equipos reales de la demo.
- **Decidido (DEC-0024):** vocabulario espacial coherente con la marca "GalaxIA" — **Star**=nodo LLM, **Satellite**=nodo de herramientas, **Node**=término neutro; Atlas=Registry, Portal=chat web, Navigator=agente/orquestador, Beacon=manifiesto, Pulse=heartbeat, Flight Log=procedencia, Mission=ejecución de tool, Orbit=conexión activa, Signal=capability (SPEC-VOCAB-0001 en `spec-native/specs/vocabulario-espacial/SPEC.md`). Sin cambios de código, contenedores ni protocolo FHS. Documento canónico publicado en `docs/vocabulario.md` y frase de posicionamiento incorporada al portal web (`site/index.md`, sección "Estrellas y satélites") — 2026-07-05. Pendiente (no bloqueante, backlog): actualización retroactiva del resto de la documentación existente (TASK-VOCAB-0005).
- **Completado y verificado:** rechazo de `hello` duplicado en el Registry (DEC-0009, primera mitad) — un `providerId` con una conexión activa (`readyState` real, no solo presencia en el mapa) ya no puede ser sobrescrito en silencio por una segunda conexión; responde `error { code: "ALREADY_REGISTERED" }` y cierra el socket nuevo. Una reconexión legítima tras una caída sigue funcionando sin bloqueo. Verificado con pruebas reales (duplicado simultáneo + reconexión tras `terminate()`). La segunda mitad de DEC-0009 (dispatcher/mosquito) sigue pendiente en `satelite-rating`.
- **Completado:** `ocr-confirmacion` — confirmación explícita (burbuja colapsada + botones) antes de que el LLM use el texto OCR (SPEC-OCRCONFIRM-0001, `spec-native/specs/ocr-confirmacion/SPEC.md`). Verificado end-to-end contra el bastion real. Sienta la base de estado por conversación que `rag-provider` también necesita (TASK-RAG-0001).
- **Completado y verificado:** topología multi-host — core (`web`+`agent-server`) en laptop (`192.168.3.137`), providers pesados (LLM/OCR) en bastion (`192.168.3.173`) (DEC-0022, `docs/despliegue-multi-host.md`). Desplegado y validado end-to-end con tráfico real cruzando ambas máquinas: chat simple y flujo completo de OCR con confirmación. Requirió además ajustar el mapeo de puertos de los providers (sin remapeo, para que coincida con lo anunciado en el manifiesto) y abrir reglas UFW en ambas máquinas, acotadas a la LAN.
- **Completado y verificado:** TLS/WSS con certificado autofirmado para todo el protocolo FHS (DEC-0023, `docs/tls-autofirmado.md`). Opt-in vía overlay `containers/compose.tls.yaml` — no afecta el despliegue sin TLS. Aplicado y validado end-to-end sobre la topología multi-host real (laptop + bastion): chat simple y flujo completo de OCR funcionando sobre el canal cifrado. Requirió tres ajustes de infraestructura (bind-mount con ruta absoluta configurable, puerto 8443 en vez de 443 por restricción de podman rootless, y recrear el túnel SSH del socket de podman) — ninguno del código TLS en sí.
- **Completado:** Pulse de transporte + timeout configurable del cliente (DEC-0010). El Registry sondea a cada nodo con ping/pong **nativo de WebSocket** (no un mensaje FHS) para detectar conexiones rotas más rápido que el lease de 30s (`apps/agent-server/src/registry/ws-handler.ts`), tolerando 3 ciclos sin `pong` antes de terminar (no 1, para no ser más estricto que el lease de aplicación); se descarta explícitamente un mensaje de "progreso" de Mission (mantiene el Registry simple, DEC-0005) — es responsabilidad del nodo resolver internamente si se atoró, sin garantía de *cuándo*; y se agrega `preferences.maxWaitMs` en `ModelPreferences` (recomendado en rango de minutos, no segundos, dado el hardware comunitario) para que quien inicia la conversación pueda pedir esperar menos que el default fijo del stack. Verificado con `agent-server` real local. Pendiente no bloqueante: exponer `maxWaitMs` como control visible en el `Portal` (mismo patrón que `ocrMode`, DEC-0021).
- **Completado (lado Agent Server):** trazabilidad operacional (DEC-0012) — `apps/agent-server/src/observability/trace.ts` loggea una línea estructurada (`conversationId`, `requestId`, `providerId`, `capability`, `dispatchMs`, `totalMs`, `success`, `errorCode`) en cada Mission/chat resuelto, en éxito, timeout, error del nodo o cierre de conexión inesperado. Verificado end-to-end: `conversationId` de una sesión de chat real correlacionado con su línea de traza. Pendiente no bloqueante: que `llm-provider`/`ocr-provider` también loggeen su propia metadata local.
- **Completado:** validación de manifiesto + códigos de error estandarizados (DEC-0013) — el Registry rechaza (`INVALID_MANIFEST`) un manifiesto sin los campos obligatorios (`fhsVersion`, `provider.id/type/visibility`, `privacy.retention`, `privacy.trainingUse` si es `llm`, `endpoint`); `FHS_ERROR_CODES` en `packages/fhs-protocol` como códigos canónicos; `llm-provider`/`ocr-provider` migrados. Al activar la validación se descubrió que ninguno de los dos providers de referencia declaraba `privacy` en su propio manifiesto — corregido como parte de esta misma iniciativa. Verificado con `agent-server` real: manifiesto incompleto rechazado, `llm-provider` con el fix completa un chat E2E sin problemas.

## Después

- **Separar Registry del Agent Backend:** convertir el Registry en un servicio independiente para soportar múltiples backends y comunidades.
- **Identidad criptográfica (Ed25519):** reemplazar DID simplificado por firmas reales en registro y heartbeat.
- **IPFS para artefactos:** subir archivos adjuntos a IPFS y pasar solo el hash a los servidores MCP. Esto protege la privacidad del origen, permite desacoplamiento temporal y deduplicación natural.
- **Autenticación de usuarios:** retomar `SPEC-AUTH-0001` cuando el MVP esté estable.
- **Modelo de confianza comunitaria:** reputación, vetos persistentes y políticas de privacidad más granulares.
- **Dispatcher/heartbeat concurrente en providers:** cada provider debe gestionar peticiones entrantes sin bloquear su propio `ping` (DEC-0009) — ahora en spec como `satelite-rating` (SPEC-SATRATING-0001), ver arriba.
- **SDK/provider de referencia en Python:** primer lenguaje no-TypeScript soportado (DEC-0011); caso de uso natural para providers de IA/OCR.
- **SDK/provider de referencia en Rust:** para hardware con recursos limitados (DEC-0011).
- **SDK/provider de referencia en Java:** para integración con sistemas comunitarios existentes (DEC-0011).
- **Evaluar modelo de chat general con tool calling** (`qwen2.5-3b-chat-q4` u otro) — hoy el modelo de producción es Coder 3B, suficiente para la demo de OCR pero no ideal para chat general (DEC-0016).

## Más adelante

- **Descubrimiento descentralizado (fase 1, en spec):** autodescubrimiento del Registry por mDNS en la LAN local, como complemento opcional a `REGISTRY_URL` manual — no reemplaza el Registry centralizado (SPEC-P2P-0001 en `spec-native/specs/p2p-discovery/SPEC.md`). No se inicia hasta decisión explícita. DHT/libp2p para descubrimiento fuera de la LAN queda como fase 2, sin spec todavía.
- **Más tipos de proveedores:** `embedding`, `storage`, `resource`, `agent`.
- **NAT traversal:** permitir nodos fuera de la red local.
- **Gateway FHS en Rust/Rust-like:** versión ligera del protocolo para equipos pequeños.
- **Marketplace de tools:** catalogar y versionar capacidades ofrecidas por la comunidad.

## No hacer por ahora

- Frameworks frontend pesados (React, Vue, Angular) en el MVP.
- Soporte multi-idioma completo en la interfaz (solo español e inglés de los modelos/tools).
- Facturación, cuotas o monetización.
- Versionado de schemas de tools MCP fuera del alcance del protocolo.
- Reescribir el protocolo en Rust u otro lenguaje hasta que TypeScript demuestre validez.
