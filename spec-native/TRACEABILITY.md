# TRACEABILITY.md

Mapa de relaciones entre specs, tareas, decisiones y validacion.

## Objetivo

Permitir que una persona o agente pueda reconstruir rapidamente:

- que spec origino un cambio
- que tareas ejecutaron esa spec
- que decisiones condicionaron el trabajo
- que evidencia valida el resultado

## Cuando actualizar este archivo

Actualizar al cerrar una iniciativa, no durante la ejecucion.
El momento correcto es cuando la spec pasa a estado `done` o `blocked`.

Si una decision cambia el alcance de una spec activa, registrar
la relacion antes de continuar.

## Formato sugerido

| Spec | Estado | Tareas | Decisiones | Archivos principales | Validacion | Observaciones |
| --- | --- | --- | --- | --- | --- | --- |
| fhs-mvp | active | TASK-FHS-0001..0008 | DEC-0001..DEC-0022 | `packages/fhs-protocol/`, `apps/agent-server/`, `apps/web/`, `examples/llm-provider/`, `examples/ocr-provider/`, `containers/compose.yaml` | Chat E2E con Qwen2.5-Coder-3B + OCR E2E con ether-ocr (validado 2026-07-02 con modelo real, PDF válido); aislamiento de eventos entre conversaciones concurrentes verificado con dos inferencias reales en paralelo | Stack desplegado en bastion 192.168.3.173. LLM via FHS WS + curl. OCR via FHS WS + curl -F. Timeouts a 300s para hardware lento. Soporte PDF en OCR. `McpHost` reescrito para hablar FHS WS real (DEC-0014). Matching de capability corregido (DEC-0015). Modelo cambiado a Qwen2.5-Coder-3B con tool calling + fallback de parseo en `llm-bridge.ts` (DEC-0016, DEC-0017). Configuración de modelo movida a env vars (DEC-0019). EventBus aislado por conversationId (DEC-0018). Ejecución determinística de OCR, sin depender de tool calling (DEC-0020), configurable vía `ocrMode` (DEC-0021). Topología multi-host diseñada y `compose.yaml` preparado, despliegue real pendiente (DEC-0022, `docs/despliegue-multi-host.md`). |
| ocr-confirmacion | done | TASK-OCRCONFIRM-0001..0005 | DEC-0020 | `packages/fhs-protocol/src/sse.ts`, `apps/agent-server/src/agent/runtime.ts`, `apps/agent-server/src/api/chat-ws.ts`, `apps/web/src/components/chat-view.ts`, `apps/web/src/services/api.ts` | Verificado 2026-07-02 contra el bastion real: texto OCR mostrado correctamente antes de responder (`"HOLA MUNDO PDF TEST..."` coincide exacto con el PDF de prueba), "Descartar" con cero llamadas al LLM confirmado en logs, "Usar documento" responde correctamente usando el texto ya extraído | Primera iniciativa en construir estado por conversación en `chat-ws.ts` (`pendingAttachments`) — reutilizable por `rag-provider` (TASK-RAG-0001). |
| rag-provider | draft | TASK-RAG-0001..0007 | DEC-0020 (precedente de diseño), DEC-0025 (separación memoria/RAG/KB, diseño cerrado) | `examples/rag-provider/` (no creado aún) | — | Solo documentado, sin implementar. Diseño cerrado el 2026-07-05: estado por conversación resuelto (vive en `chat-ws.ts`, mismo patrón que `pendingAttachments` de `ocr-confirmacion`), disparo de indexado solo por evento confirmado, retención/privacidad generalizadas. Ya no quedan riesgos de diseño abiertos, solo falta priorizar frente al resto del backlog. |
| kb-provider | draft | TASK-KB-0001..0003 | DEC-0025 (origen: separación memoria/RAG/KB), DEC-0027 (disparador), DEC-0028 (tags de proveedor/comunidad), DEC-0029 (ID de dispositivo, sin resolver) | `examples/kb-provider/` (no creado aún), `packages/fhs-protocol/src/types.ts` (`Capability.tags`, pendiente TASK-KB-0003) | — | Solo documentado, sin implementar. Diseño del disparador cerrado el 2026-07-06 (DEC-0027): modo manual y recomendado dentro de alcance; modo "mágico" documentado como premisa a futuro, no implementado (mismo riesgo que DEC-0020 resolvió para OCR). Tags autodeclarados de proveedor diseñados sin bloqueos (TASK-KB-0003); tags de comunidad diseñados pero bloqueados por `SPEC-AUTH-0001` (DEC-0028) — se documentó (DEC-0029, `proposed`, sin resolver) la idea de un ID de dispositivo como validación intermedia, pendiente de debate más profundo antes de decidir si se construye. Falta TASK-KB-0002 (proceso administrativo de indexado) antes de escribir código. |
| satelite-rating | done (local) / blocked (hw real) | TASK-SATRATING-0001..0008 | DEC-0009 (dispatcher concurrente, formalizado ahora), DEC-0024 (vocabulario) | `packages/fhs-protocol/src/messages.ts`, `apps/agent-server/src/registry/metrics.ts` (nuevo), `apps/agent-server/src/registry/registry.ts`, `apps/agent-server/src/agent/runtime.ts`, `apps/agent-server/src/providers/mcp-host.ts`, `apps/agent-server/src/providers/llm-gateway.ts`, `apps/agent-server/src/api/providers.ts`, `examples/llm-provider/src/index.ts`, `examples/ocr-provider/src/index.ts` | Verificado 2026-07-05 con stack local real (agent-server + llm-provider + ocr-provider reales, FHS WebSocket real, LLM/OCR mockeados vía HTTP): chat simple con `avgDispatchMs: 6, avgTotalMs: 34, rating: 5`; OCR con confirmación con `avgDispatchMs: 0, avgTotalMs: 17, rating: 5`. Caso borde: LLM mock reintentó `ocr_extract` tras confirmar (decide por palabras clave, no por tools ofrecidas) — runtime respondió `tool.error` sin registrar muestra falsa, sin romper la conversación (confirma que el fix de e7f7e64 sigue vigente) | Formaliza el "mosquito" (DEC-0009) con un mensaje `dispatch.ack` verificable, y agrega historial de latencia + rating por nodo en el Registry, expuesto en `GET /api/fhs/providers`. El rating se expone, no se usa aún para resolución de providers (fuera de alcance de esta iteración). **Bloqueado** (2026-07-05): repetir verificación contra los 3 equipos reales de la demo requiere acceso a la infraestructura física, no disponible en este entorno de trabajo — no bloquea el resto del roadmap. |
| (fix aislado, sin spec dedicada) | done | — | DEC-0009 (primera mitad: validación de identidad) | `packages/fhs-protocol/src/messages.ts`, `apps/agent-server/src/registry/registry.ts`, `apps/agent-server/src/registry/ws-handler.ts`, `examples/llm-provider/src/index.ts`, `examples/ocr-provider/src/index.ts` | Verificado 2026-07-05 con dos pruebas reales contra el agent-server local: hello duplicado simultáneo rechazado (`ALREADY_REGISTERED`, socket cerrado con código 4009) sin afectar la conexión original; reconexión legítima tras `ws.terminate()` aceptada sin bloqueo | P0 del plan de priorización — quick win aislado antes de iniciar `satelite-rating`. La segunda mitad de DEC-0009 (dispatcher/mosquito) sigue pendiente ahí. |
| vocabulario-espacial | in_progress | TASK-VOCAB-0001..0005 | DEC-0024 | `spec-native/specs/satelite-rating/SPEC.md`, `spec-native/specs/p2p-discovery/SPEC.md` (ya alineadas), `docs/vocabulario.md` (publicado), `site/index.md` (publicado) | Decisión tomada 2026-07-05 (Opción B): Star=LLM, Satellite=tools, Node=neutro. `satelite-rating`/`p2p-discovery` y sus `TASKS.md` actualizados a este vocabulario. Documento canónico y frase de posicionamiento verificados por revisión manual — sin build real de Jekyll disponible en este entorno | Solo queda TASK-VOCAB-0005 (backlog no bloqueante: actualización retroactiva del resto de la documentación existente). La reformulación de FHS (TASK-VOCAB-0004b) y el renombrado de código/contenedores (fuera de alcance) se resolvieron/registraron aparte. |
| (fix aislado, sin spec dedicada) | done | — | DEC-0010 (Pulse de transporte + timeout configurable) | `apps/agent-server/src/registry/ws-handler.ts`, `apps/agent-server/src/agent/runtime.ts`, `apps/agent-server/src/providers/mcp-host.ts`, `apps/agent-server/src/providers/llm-gateway.ts`, `docs/protocolo.md` | `npm run typecheck -w apps/agent-server` — OK. Verificado 2026-07-05 con `agent-server` real local + un cliente `ws` crudo simulando un provider: conexión abierta 25s (2.5 ciclos de `HEARTBEAT_INTERVAL_SECONDS`) sin enviar ningún `ping` de aplicación — el Pulse nativo de WS la mantuvo viva (2 pings nativos recibidos, socket nunca terminado) | Resuelve DEC-0010 con tres partes: Pulse nativo de WS bidireccional (reemplaza la idea original de un segundo mensaje FHS), decisión explícita de NO agregar señal de progreso de Mission (responsabilidad del nodo), y `preferences.maxWaitMs` como timeout configurable del cliente. Pendiente no bloqueante: UI en el Portal para `maxWaitMs`, y verificar que una conexión que de verdad no responde al ping nativo (proceso colgado) se cierra correctamente — solo se probó el caso "vivo". |
| (fix aislado, sin spec dedicada) | done | — | DEC-0030 (identidad Ed25519, reemplaza DEC-0004) | `packages/fhs-protocol/src/identity.ts` (nuevo), `packages/fhs-protocol/src/constants.ts` (`INVALID_SIGNATURE`), `packages/fhs-protocol/src/messages.ts`, `apps/agent-server/src/registry/ws-handler.ts`, `examples/llm-provider/src/index.ts`, `examples/ocr-provider/src/index.ts`, `examples/{llm,ocr}-provider/src/identity-store.ts` (nuevos), `docs/protocolo.md`, `docs/protocolo-provider.md`, `docs/manifiesto-llm.md` | `npm run typecheck` limpio en los 3 paquetes. Verificado 2026-07-06: `did:key:z6Mk...` real generado y estable entre reinicios; chat E2E funcionando; ataque de suplantación simulado (nodo con clave propia anunciándose con el `providerId` de otro) rechazado con `INVALID_SIGNATURE` | El `providerId` deja de ser un nombre elegido a mano — es la clave pública Ed25519 codificada (`did:key` W3C completo, no una versión simplificada). Usa `node:crypto` nativo, sin dependencias nuevas (ni siquiera para base58, implementado localmente). DEC-0009 (rechazo de `hello` duplicado) queda como defensa complementaria, ya no la principal contra suplantación. Sin mecanismo de recuperación si se pierde la clave privada — limitación conocida, documentada explícitamente. |
| (fix aislado, sin spec dedicada) | proposed (sin implementar) | — | DEC-0031 (nivel "premium" de nodo) | ninguno todavía | — | Documentado, sin código. Tres piezas de naturaleza distinta: campos declarativos (URL de código fuente, licencia, mantenedores, SLA, hardware, política de datos), mTLS entre ambos extremos, y proceso de aprobación humana. Tensión de centralización explícita frente a DEC-0030 (identidad autogenerada sin autoridad central). Cómo garantizar que el servicio en ejecución corresponde al código publicado queda explícitamente pendiente de debate más profundo, sin solución técnica asignada. |
| p2p-discovery | done (local) | TASK-P2P-0001..0006 | DEC-0001 (reafirma la decisión de v0.1, fase 1 acotada), DEC-0024 (vocabulario), DEC-0030 (identidad Ed25519, reutilizada), DEC-0032 (librería + identidad del Registry) | `apps/agent-server/src/registry/mdns-announce.ts` (nuevo), `apps/agent-server/src/registry/identity-store.ts` (nuevo), `apps/agent-server/src/index.ts`, `examples/{llm,ocr}-provider/src/index.ts`, `examples/{llm,ocr}-provider/src/registry-discovery.ts` (nuevos) | `npm run typecheck` limpio en los 3 paquetes. Verificado 2026-07-06: `agent-server` anuncia con `did:key` propio; `llm-provider` sin `REGISTRY_URL` lo descubre, verifica la firma, arma la URL y se registra (`GET /api/fhs/providers` lo confirma); con `REGISTRY_EXPECTED_DID` apuntando a un `did` inexistente, rechaza correctamente con mensaje claro | Autodescubrimiento del Registry por mDNS en la LAN, como fallback opcional a `REGISTRY_URL` manual — no reemplaza el Registry centralizado ni cambia el protocolo `hello`/`register`/Pulse. Librería (`bonjour-service`) elegida con datos reales (descargas, bindings nativos, tipos TS, mantenimiento), no solo criterio. El Registry ahora tiene su propia identidad Ed25519 (DEC-0032, extiende DEC-0030) — el anuncio mDNS va firmado, con anclaje opcional (`REGISTRY_EXPECTED_DID`). Pendiente: TASK-P2P-0005 (documentación) y TASK-P2P-0006, **bloqueada por falta de hardware real** (mismo bloqueo que issue #1). DHT/libp2p fuera de la LAN queda fuera de alcance, sin spec todavía. |
| (fix aislado, sin spec dedicada) | done (lado Agent Server) | — | DEC-0012 (trazabilidad operacional) | `apps/agent-server/src/observability/trace.ts` (nuevo), `apps/agent-server/src/providers/mcp-host.ts`, `apps/agent-server/src/providers/llm-gateway.ts`, `apps/agent-server/src/agent/runtime.ts`, `docs/protocolo.md`, `docs/protocolo-provider.md` | `npm run typecheck -w apps/agent-server` — OK. Verificado 2026-07-05 con `agent-server` + `llm-provider` reales contra un LLM mock: línea de traza `{conversationId, requestId, providerId, capability, dispatchMs, totalMs, success}` correlacionada correctamente con el `conversationId` real de la sesión de chat | Cierra el gap de DEC-0012 del lado del Agent Server — cubre éxito, timeout, error del nodo y cierre de conexión inesperado en los 3 puntos de despacho (OCR determinístico, tool call vía LLM, chat). Pendiente no bloqueante: que `llm-provider`/`ocr-provider` también loggeen su propia metadata local por `requestId`. |
| (fix aislado, sin spec dedicada) | done | — | DEC-0013 (validación de manifiesto + códigos de error) | `packages/fhs-protocol/src/constants.ts` (`FHS_ERROR_CODES`), `apps/agent-server/src/registry/manifest-validation.ts` (nuevo), `apps/agent-server/src/registry/ws-handler.ts`, `examples/llm-provider/src/index.ts`, `examples/ocr-provider/src/index.ts`, `docs/protocolo-provider.md` | `npm run typecheck` limpio en los 4 paquetes. Verificado 2026-07-06: manifiesto sin `privacy.retention` rechazado con `INVALID_MANIFEST` y detalle de campos faltantes; `llm-provider` real completa un chat E2E contra un LLM mock sin problemas tras el fix | Al activar la validación se descubrió que ni `llm-provider` ni `ocr-provider` declaraban `privacy` en su propio manifiesto — se habrían rechazado a sí mismos. Corregido como parte de esta misma iniciativa (mismo patrón que la lección de DEC-0016/DEC-0017: "documentado" no es "implementado" hasta verificarlo con una ejecución real). |
| (fix aislado, sin spec dedicada) | done (Fase 1+2) | — | DEC-0033 (rename de código a nomenclatura Galaxy/IA, Fase 1+2 de DEC-0024) | `packages/fhs-protocol/src/types.ts`, `packages/fhs-protocol/src/manifest.ts`, `apps/agent-server/src/atlas/` (renombrado desde `src/registry/`), `examples/star-example/` (renombrado desde `examples/llm-provider`), `examples/satellite-ocr-example/` (renombrado desde `examples/ocr-provider`), `containers/star/`, `containers/satellite-ocr/`, `containers/compose.yaml`, `containers/compose.tls.yaml`, `Justfile`, `helpers/mk/*.mk`, `helpers/just/*.just` | `npm run typecheck --workspaces` y `npm run build --workspaces` limpios tras el rename completo. Verificado real: `agent-server` (Atlas) + `examples/star-example` levantados en local, registro exitoso vía WS firmado (`GET /api/fhs/providers`), chat E2E completo (`POST /api/chat`) resuelto con traza correcta | El protocolo JSON en el cable no cambió — mismo `providerId`, mismos tipos de mensaje (`hello`/`register`/etc.). Solo cambian identificadores TS, paths de archivo, nombres de paquete npm y de contenedor. Fase 3 (renombrar el protocolo JSON en sí) queda fuera de alcance, documentada como pendiente en DEC-0033. |
| (fix aislado, sin spec dedicada) | done | — | DEC-0034 (extiende DEC-0033: `agent-server`→`navigator`, `web`→`portal`) | `apps/navigator/` (renombrado desde `apps/agent-server`), `apps/portal/` (renombrado desde `apps/web`), `containers/navigator/`, `containers/portal/`, `containers/compose.yaml`, `containers/compose.tls.yaml`, `containers/star/Containerfile`, `containers/satellite-ocr/Containerfile` (fix del `REGISTRY_URL` default), `docs/navigator.md` (renombrado desde `docs/agent-server.md`) | `npm run typecheck --workspaces` y `npm run build --workspaces` limpios. Verificado real: `apps/navigator` levantado, `examples/star-example` se registra y un chat E2E se resuelve con traza correcta | Resuelve la ambigüedad que DEC-0033 había dejado abierta (esa app hospeda tanto Atlas como Navigator) asignándole el nombre del rol dominante. De paso corrige un bug latente: el `REGISTRY_URL` default de los Containerfiles de `star`/`satellite-ocr` apuntaba a un nombre de servicio Docker (`agent-server`) que ya no existía. Separar Atlas del Navigator en servicios independientes: resuelto en DEC-0035. |
| (fix aislado, sin spec dedicada) | done | — | DEC-0035 (separar Atlas de Navigator en servicios independientes) | `apps/atlas/` (nuevo, absorbe `apps/navigator/src/atlas/*` + `api/providers.ts`), `apps/navigator/src/atlas-client.ts` (nuevo), `apps/navigator/src/agent/runtime.ts` (resolución async + fire-and-forget), `apps/navigator/src/index.ts`, `apps/navigator/src/api/chat.ts`, `apps/navigator/src/api/chat-ws.ts`, `containers/atlas/Containerfile` (nuevo), `containers/navigator/Containerfile`, `containers/compose.yaml`, `containers/compose.tls.yaml`, `containers/portal/nginx.conf`, `containers/portal/nginx-tls.conf`, `docs/atlas.md` (nuevo), `docs/navigator.md` | `npm run typecheck --workspaces` y `npm run build --workspaces` limpios en las 6 workspaces. Verificado real con 3 procesos `tsx` separados (atlas :8081, navigator :8083 con `ATLAS_URL`, star-example registrándose contra atlas): `POST /api/chat` resolvió el provider vía HTTP a Atlas, el chat se completó, y la muestra de métrica llegó a Atlas (`GET /api/fhs/providers` mostró rating/latencia actualizados tras la llamada) | El único acoplamiento real en tiempo de ejecución eran dos llamadas (`getProviders`/`recordSample`) — el resto (LlmGateway, McpHost, el WS de registro) ya eran autocontenidos. Los eventos `node.online`/`node.lost` resultaron ser código muerto (nadie llamaba `subscribeToRuntime()`) — no había nada que puentear entre procesos. Gap conocido, no resuelto: `AtlasClient` no soporta TLS con certificados autofirmados (`fetch` sin override de confianza), documentado en `compose.tls.yaml`. Pendiente: verificación real en topología de dos máquinas, bloqueada por hardware (mismo caso que issue #1). |
| satelite-rating | done (persistencia) | — | DEC-0036 Parte 1 (SQLite+WAL para el rating) | `apps/atlas/src/atlas/metrics.ts` (reescrito, misma API pública), `apps/atlas/src/atlas/registry.ts` (constructor recibe `metricsDbPath`), `apps/atlas/src/index.ts` (`ATLAS_DB_PATH`), `apps/atlas/package.json` (`better-sqlite3`), `containers/atlas/Containerfile` (build tools para compilar el binding nativo), `containers/compose.yaml` (volumen `atlas-data`), `.gitignore` | `npm run typecheck`/`build --workspaces` limpios. Verificado real: registrar un Star, generar una muestra de rating con un chat real, **matar el proceso de Atlas y reiniciarlo** apuntando al mismo `ATLAS_DB_PATH`, confirmar `GET /api/fhs/providers` con el mismo `sampleCount` sin ninguna llamada nueva. Archivos `.db-wal`/`.db-shm` confirmados presentes (WAL activo) | Excepción consciente al precedente de DEC-0032 (bindings nativos rechazados por hardware comunitario variado) — aquí Atlas corre en la máquina core que el operador controla, no en hardware donado. El catálogo de nodos se queda en memoria a propósito (correcto que no persista, se reconstruye vía reconexión automática de providers). |
| (sin spec dedicada, ver SPEC-BRIDGE-0001) | proposed (solo spec) | — | DEC-0036 Parte 2 (puente Atlas↔Navigator vía NATS, sin implementar) | `spec-native/specs/atlas-navigator-bridge/SPEC.md` (nuevo) | N/A — no hay código que verificar, es un documento de diseño | Retoma el hallazgo de DEC-0035 (eventos `node.online`/`node.lost` sin consumidor real) y propone NATS como bus entre procesos. Preguntas abiertas (NATS core vs. JetStream, snapshot inicial vs. deltas) sin resolver — mismo patrón que DEC-0027/0029/0031: premisa documentada, no implementada. |

## Trazabilidad operacional (runtime) — no confundir con la tabla anterior

La tabla de arriba es trazabilidad **de repositorio**: qué spec/decisión originó qué código. Es distinta de la trazabilidad **de ejecución**: poder reconstruir qué pasó con una petición real de un usuario (`conversationId` → `requestId` → provider → resultado) para diagnosticar un fallo, sin violar la privacidad de contenido.

Esa segunda trazabilidad está definida como requisito de protocolo en `docs/protocolo.md` (sección "Privacidad → Trazabilidad operacional") y `docs/protocolo-provider.md` (checklist plug-and-play). **Cerrada del lado del Agent Server el 2026-07-05** (DEC-0012, ver fila de arriba) — sigue pendiente, no bloqueante, que cada provider de ejemplo loggee su propia metadata local.

## Lección de esta iniciativa: "registrado" no es "probado"

El 2026-07-02 se probó por primera vez el pipeline de OCR de punta a punta contra el bastion real (no solo build/typecheck/registro en el Registry) y aparecieron varios bugs que llevaban tiempo sin detectarse: DEC-0014 (transporte incorrecto en `McpHost`), DEC-0015 (matching de capability roto), DEC-0016/DEC-0017 (ningún modelo con tool calling confiable, y `llama-server` no siempre llenaba `tool_calls` aunque el modelo decidiera usarlo) y DEC-0018 (el `EventBus` mezclaba eventos entre conversaciones concurrentes — encontrado por accidente al correr dos pruebas seguidas). Todos eran invisibles desde el Registry — el provider se veía `online` con su capability declarada, el build pasaba, el typecheck pasaba. Solo pruebas end-to-end reales (incluyendo, para DEC-0018, con **concurrencia real**) expusieron cada problema.

**Regla derivada para integrar piezas nuevas de aquí en adelante:** ninguna integración (provider nuevo, modelo nuevo, capability nueva) se considera "lista" solo porque se registra en el Registry o porque compila. Requiere al menos una ejecución end-to-end real (petición → tool call o chat.request → respuesta) antes de marcar la tarea `done` en `spec-native/tasks/`. Para funcionalidad que involucra múltiples clientes/conexiones (como el chat), una prueba de un solo cliente no es suficiente — DEC-0018 solo se detectó con dos conversaciones simultáneas reales. Ver `docs/protocolo-provider.md` sección "Lecciones de integración" para el detalle de cada bug.
